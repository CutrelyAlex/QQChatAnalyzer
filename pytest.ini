[pytest]
# Only collect the chat_import-focused tests (per current project direction).
testpaths = tests
python_files = test_chat_import_*.py

# Register custom markers to avoid warnings.
markers =
    slow: slow tests (e.g. large fixtures like group_test.json)
